# Go build output
/bin/
*.test

# Node
/web/node_modules/
/web/dist/

# Local env (never commit secrets)
.env
*.local

# Uploaded files (the directory itself is tracked via uploads/.gitignore)
/uploads/*
!/uploads/.gitignore

# OS / editor
.DS_Store
